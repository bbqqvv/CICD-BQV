name: Node.js CI


on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  build:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
    - uses: actions/checkout@v4
    
    - name: Sử dụng Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20.x'
        cache: 'npm'
        
    - name: Cài đặt dependencies
      run: npm install
      
    - name: Kiểm tra chất lượng code (Lint)
      run: npm run lint
      
    - name: Chạy các bài kiểm tra (Tests)
      run: npm test
      env:
        APP_SECRET: ${{ secrets.MY_APP_SECRET }}

    - name: Chuyển đổi tên repository sang viết thường
      run: |
        echo "IMAGE_NAME=ghcr.io/${GITHUB_REPOSITORY,,}" >> $GITHUB_ENV

    - name: Đăng nhập vào GitHub Container Registry
      uses: docker/login-action@v3
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Thiết lập Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Build và Push Docker image lên GHCR
      uses: docker/build-push-action@v5
      with:
        context: .
        push: true
        tags: |
          ${{ env.IMAGE_NAME }}:latest
          ${{ env.IMAGE_NAME }}:${{ github.sha }}
